spring:
  datasource:
    url: "jdbc:h2:mem:my_lib"
    username: "sa"
    password: ""
    driver-class-name: "org.h2.Driver"
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration,classpath:db/seeds
